pipeline {
    agent any
    environment {
        // Definir la variable de entorno para el token de SonarQube
        SONARQUBE_TOKEN = credentials('sonarqube')
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('SonarQube Analysis') {
            steps {
                script {
                    withSonarQubeEnv('SonarQube') {
                        sh '''
                        sonar-scanner \
                          -Dsonar.projectKey=tpintegrador \
                          -Dsonar.sources=. \
                          -Dsonar.host.url=http://your-sonarqube-server \
                          -Dsonar.login=${env.SONARQUBE_TOKEN}
                        '''
                    }
                }
            }
        }
    }
}
